class Solution {
public:
    vector<int> grayCode(int n) {
        vector<int> result((1 << n), 0);

        while (n--) {
            for (int i = (1 << n); i < ; ++i) {

            }
        }
    }
};